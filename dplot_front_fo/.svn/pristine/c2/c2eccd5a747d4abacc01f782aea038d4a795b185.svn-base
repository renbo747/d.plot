.the-header {
  position: fixed;
  top: 0;
  left: 0;
  display: block;
  width: 100%;
  z-index: 1000;
  &.show,
  &.on {
    display: block;
    background: #222;
  }
  &.hide {
    display: none;
  }
  .the-header__inner {
    display: flex;
    justify-content: space-between;
    align-items: center;
    width: 100%;
    max-width: 768px;
    height: 50px;
    margin: 0 auto;
    padding: 0 20px;
  }
  .logo {
    display: flex;
    align-items: center;
    width: 80px;
    height: 18px;
    margin-bottom: 0;
  }
  .logo a {
    font-size: 22px;
    color: #fff;

    &.black {
      color: $black;
    }
  }
  .menu {
    display: flex;
    align-items: center;
    line-height: 1;
    .menu-icon + .menu-icon {
      margin-left: 14px;
    }

    .menu-icon.cart {
      position: relative;
      &:before {
        display: flex;
        align-items: center;
        content: attr(data-count);
        position: absolute;
        top: 0;
        right: 0;
        transform: translate(50%, -50%);
        font-size: 12px;
        padding: 0 6px;
        text-align: center;
        height: 18px;
        background-color: #ccff00;
        border-radius: 9px;
        font-family: 'AttenNew', sans-serif;
        color: #000;
        font-style: normal;
        white-space: nowrap;
      }
    }
  }
  .menu-icon  {
    display: inline-flex;
    width: 20px;
    height: 20px;
    &:last-child {
      margin-right: 0;
    }
  }
  .menu-icon__search {
    cursor: pointer;
    background: url('~@/assets/mobile/img/icon/icon-search-white-20px.svg') no-repeat left 0;
  }
  .menu-icon__cart {
    cursor: pointer;
    background: url('~@/assets/mobile/img/icon/icon-cart02-white-20px.svg') no-repeat left 0;
  }

  .menu-icon__profile {
    cursor: pointer;
    background: url('~@/assets/pc/img/icon/icon-profile-white-28px.svg') no-repeat left 0;
  }
  .the-header__left {
    display: flex;
    align-items: center;
    line-height: 1;
  }
  .the-header__prev {
    margin-right: 10px;
  }
  .the-header__title {
    font-size: 19px;
    font-weight: 400;
    color: #fff;
    margin-bottom: 0;
  }

  // 검색 헤더
  &.search-header {
    .the-header__inner {
      justify-content: inherit;
    }

    .search-container {
      position: relative;
      width: 100%;
    }

    .form-control {
      height: 38px;
    }

    // 브랜드 검색결과
    .search-live {
      position: absolute;
      top: 44px;
      left: 0;
      width: calc(100% + 70px);
      height: calc((var(--vh, 1vh) * 100) - 110px);
      margin-left: -50px;
      padding-top: 20px;
      padding-left: 20px;
      padding-right: 20px;
      background-color: $white;

      .brand-header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding-bottom: 20px;
        margin-bottom: 30px;
        border-bottom: 1px solid $gray-400;
      }

      .brand {
        display: flex;
        align-items: center;
        flex-direction: column;
      }

      .brand-logo__img {
        width: 100px;
        height: 34px;
        margin-bottom: 10px;

        img {
          height: 100%;
        }
      }

      .brand-name {
        margin-bottom: 0;
        font-size: 14px;
        letter-spacing: -.56px;
        color: $gray-700;
      }

      .search__list {
        li {
          font-size: 14px;
          letter-spacing: -.56px;
          color: $gray-700;
        }

        li + li {
          margin-top: 10px;
        }

        .search-word {
          color: $primary;
        }
      }

    }
  }
}
